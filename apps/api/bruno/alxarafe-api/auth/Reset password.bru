meta {
  name: Reset password
  type: http
  seq: 42
}

headers {
  X-CSRF-Token: {{csrfToken}}
}

post {
  url: {{BASE_URL}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "{{resetToken}}",
    "password": "{{RESET_PASSWORD}}"
  }
}
