meta {
  name: Reset password
  type: http
  seq: 42
}

post {
  url: http://{{BASE_URL}}/auth/reset-password
  body: json
  auth: none

  headers {
    X-CSRF-Token: {{csrfToken}}
  }
}

body:json {
  {
    "token": "{{resetToken}}",
    "password": "{{NEW_PASSWORD}}"
  }
}