meta {
  name: Forgot password
  type: http
  seq: 40
}

post {
  url: http://{{BASE_URL}}/auth/forgot-password
  body: json
  auth: none

  headers {
    X-CSRF-Token: {{csrfToken}}
  }
}

body:json {
  {
    "email": "{{EMAIL}}"
  }
}