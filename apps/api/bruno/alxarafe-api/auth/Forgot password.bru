meta {
  name: Forgot password
  type: http
  seq: 40
}

headers {
  X-CSRF-Token: {{csrfToken}}
}

post {
  url: {{BASE_URL}}/auth/forgot-password
  body: json
  auth: none
}

body:json {
  {
    "email": "{{EMAIL}}"
  }
}
