meta {
  name: Verify email
  type: http
  seq: 34
}

headers {
  X-CSRF-Token: {{csrfToken}}
}

post {
  url: {{BASE_URL}}/auth/verify-email
  body: json
  auth: none
}

body:json {
  {
    "token": "{{verifyToken}}"
  }
}
